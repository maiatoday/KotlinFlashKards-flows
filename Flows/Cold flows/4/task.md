**What kind of operator can we use to change the emissions of a flow?**

<div class="hint">
  an intermediate operator, it will transform the flow into another flow. It doesn't start collecting until a terminal operator is called.
</div>
